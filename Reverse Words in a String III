class Solution:
    def reverseWords(self, s: str) -> str:
        words = s.split()
        result_words = []
        for word in words:
            result_words.append(word[::-1])
        return ' '.join(result_words)
