class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        hash = {}
        for i in nums:
            try:
                hash.pop(i)
            except:
                hash[i] = 1
        return hash.popitem()[0]
